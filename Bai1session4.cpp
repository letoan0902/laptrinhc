#include<stdio.h>
int main(){
	printf("Hay nhap vao mot so nguyen ");
	int n;
	scanf("%d", &n);
	if (n>0) {
		printf(" So nhap vao la so duong");
		 
	} else {
		printf("So nhap vao la so am");
	} 
	return 0; 
} 
